---
description: 开造主设计系统负面清单。默认禁止品牌渐变、彩色主层级、分割线和重阴影。
globs: "**/*.dart"
alwaysApply: true
---

# Forbidden

## 色彩

- ❌ 禁止紫色、蓝色等彩色作为大面积背景
- ❌ 禁止品牌渐变 Hero / 渐变按钮 / 渐变卡片
- ❌ 禁止用彩色文字制造层级
- ✅ 默认使用黑白灰与 surface 层级

唯一例外：
- 只允许 `#000000 -> #3C3B3B` 的极轻 tonal shift，不算普通品牌渐变
- 只用于 Hero / Main CTA

## 分隔

- ❌ 禁止 `Divider()`
- ❌ 禁止标准 1px 实线切 section
- ❌ 禁止列表项之间画线
- ✅ 用背景层级切换和大留白分隔

## 阴影

- ❌ 禁止常规卡片使用明显阴影
- ❌ 禁止 glow / neon / 彩色阴影
- ✅ 浮层才允许 24px - 40px / 4% - 6% 的 ambient shadow

## 形状

- ❌ 禁止圆形图标容器
- ❌ 禁止到处用圆角胶囊
- ✅ 默认使用 8px - 12px rounded square geometry
- ✅ 头像是唯一允许圆形的元素

## 排版

- ❌ 禁止所有标题做成平均字号梯度
- ❌ 禁止正文和说明文都用同一种强调方式
- ❌ 禁止为了“科技感”滥用大写和字距
- ✅ 用大级差标题、蓝图式 micro-label、长留白建立气质

## 布局

- ❌ 禁止所有内容居中堆成一列
- ❌ 禁止 bootstrap 式均匀卡片网格
- ❌ 禁止信息密排
- ❌ 禁止把首页直接做成通用 dashboard
- ✅ 用非对称、错位、长留白做 editorial 构图

## 组件

- ❌ 禁止在基础操作按钮和基础输入场景绕开 `VccButton` / `VccInput`
- ❌ 禁止系统 `SnackBar`
- ❌ 禁止裸用 `Card`
- ❌ 禁止在文案里继续混用 `发起人 / 造物者`
- ✅ 基础件优先共享，复杂业务块放 `features/*/widgets`
