---
description: Flutter UI 健壮性检查。禁止溢出、白屏、文本越界和小屏断裂。
globs: "**/*.dart"
alwaysApply: true
---

# UI Safety

所有 Flutter UI 改动都必须先满足这组约束，再谈视觉表现。

## 必检项

- 禁止出现 `RenderFlex overflowed`
- 禁止出现白屏、约束冲突、布局抖动、不可点击区域
- 禁止标题、金额、标签、按钮文案、状态徽标发生裁切或越界
- 默认检查 `compact` 小屏和主流手机宽度，确认选中态、展开态、加载态都稳定

## 实现要求

- 优先自然高度布局，不要依赖固定高度硬撑内容
- 谨慎使用 `Stack` 和 `Positioned`，避免多层绝对定位互相挤压
- `Row` / `Column` 中的文本默认考虑长文案，必要时使用 `Expanded`、`Flexible`、`Wrap`
- 对关键文本显式设置 `maxLines`、`TextOverflow.ellipsis` 或允许换行
- 动效切换不能破坏约束关系，展开收起和选中态都要实机检查

## 交付门槛

- 代码通过 analyze 不算完成
- 必须实际运行页面，确认没有 overflow、黄黑条、裁切和白屏
